numpy>=1.24
networkx>=3.0
matplotlib>=3.6
scipy>=1.10   # kiểm định thống kê (Wilcoxon, khoảng tin cậy t) cho run_statistics
osmnx>=1.6   # tải bản đồ thật từ OpenStreetMap; nếu thiếu -> tự fallback sang lưới mô phỏng
